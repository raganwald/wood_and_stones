-# board/_board.html.haml
- image_path_arr, tile_img_class_arr = tile_img_src_array(board), tile_img_class_array(board)
- (0..(board.dimension - 1)).each do |down|
  .row
    - (0..(board.dimension - 1)).each do |across|
      %img{ :src => image_path_arr[across][down], :class => tile_img_class_arr[across][down] }>